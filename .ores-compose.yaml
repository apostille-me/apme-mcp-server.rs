schema_version: ores.compose.v1
project: apme-mcp-server.rs
allow_lazy_start: false

services:
  apme-mcp-server.rs:
    name: apme-mcp-server.rs
    runtime: host
    build:
      - ["cargo", "build", "--bins"]
    command: ["./target/debug/apme-mcp-server.rs"]
